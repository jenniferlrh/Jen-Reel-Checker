// Cover/thumbnail text rules used by /api/analyze-video.
// Replaced by researched rules — see docs/jen-ads-brain.md section 11.
export const COVER_RULES = `封面文字通用原则（先用基础版，研究版会覆盖）：
- 主标题 6-12 个字最好，超过 15 个字会看不清
- 一眼看懂 > 文采，用大白话
- 数字 + 具体 > 形容词（"8个项目" 比 "很多项目" 强）
- 制造信息差/悬念，但不要标题党到答非所问
- 手机上缩略图很小，字要够大、对比够强
- 底部约 20% 会被平台 UI（标题/按钮）挡住，重要的字别放太低`
